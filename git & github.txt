                                   //06.04.24//
Open terminal
Then new terminal
git init
git add .
git commit -m "update"
for 1st time:
   git config --global user.email "ur email"
   git config --global user.name "ur username"
Do again git commit -m "update"

Goto Github.com create an account and repository.
Login in vscode local using github,
when u do push it will open one login prompt,login there and give i agree authorization.

git remote add origin "https://link"
git push -u origin master/main

                           //12.04.24//
Github: Github is an online platform (website) used to deploy,share and maintain the code.

Main folder should have index.jhtml
Goto repository=> settings=>pages(left side)=>select branch(right side)=>click save and wait for some time.